{
  "name": "@repo/trpc",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server/index.ts",
    "./client": "./src/client/index.ts"
  },
  "scripts": {
    "build": "echo 'No compile step — consumed as TS source'",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "@trpc/server": "^11.0.0",
    "drizzle-orm": "^0.41.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
